[pytest]
; The bundled scrapers are a self-contained uv project (packages/scrapers/
; pyproject.toml); their tests import httpx/hishel/... from that venv and run
; via: uv run --project packages/scrapers pytest. Root collection covers the
; rest of the repo's python.
norecursedirs = packages/scrapers node_modules .git
