---
phase: 01-skill-audit-prune-rc-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json
  - .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md
autonomous: false

requirements: [SKILLS-01]

must_haves:
  truths:
    - "Audit subagent processes the 41 rubric candidates against the LOCKED reference snapshot, not against the in-progress pruned state"
    - "5-skill calibration pre-flight runs FIRST and 5/5 agreement is required to proceed to the full pass"
    - "PRUNE-VERDICT.json contains every candidate verdict with per-clause evidence and `lines_cited`"
    - "Cruft removals (configure-ecc, continuous-learning loser) are logged in a separate category from training-duplicate removals"
    - "Reference snapshot SHA is captured BEFORE any prune decisions and embedded at the top of PRUNE-VERDICT.json"
  artifacts:
    - path: ".planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md"
      provides: "The locked subagent prompt with 64-skill protected list, two-pass rubric, calibration mandate, and the frozen-snapshot clause-c requirement embedded verbatim"
      contains: "reference_snapshot_sha"
    - path: ".planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json"
      provides: "Per-candidate verdicts with calibration_results.proceed=true and verdicts[] populated for all 41 candidates + cruft determinations"
      contains: "calibration_results"
  key_links:
    - from: "audit-subagent-prompt.md"
      to: "PRUNE-VERDICT.json schema (D-18)"
      via: "subagent emits structured JSON matching the locked schema"
      pattern: "reference_snapshot_sha"
    - from: "Plan 01 (this plan) reference graph snapshot"
      to: "Plan 02 human review pass + Plan 03 prune execution"
      via: "PRUNE-VERDICT.json with proceed=true is the input to human review"
      pattern: "verdicts"
---

<objective>
Author the audit subagent prompt with the locked rubric, run the 5-skill calibration pre-flight, and (only if calibration agreement is 5/5) run the full 41-candidate audit pass. Emit `PRUNE-VERDICT.json` matching the D-18 schema verbatim, with every per-clause evidence field populated and the `reference_snapshot_sha` captured against the pre-prune repo state.

Purpose: This is the decision-grade audit that produces the input to human review (Plan 02) and prune execution (Plan 03). The two-pass rubric (Pass 1: cruft filter, Pass 2: training-duplicate three-clause test) and the frozen-snapshot clause-c evaluation order (D-13) are the most subtle correctness requirements in the entire phase — they MUST be embedded verbatim in the subagent prompt, not paraphrased.

Output:
1. `.planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md` — the locked subagent prompt
2. `.planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json` — the audit output with calibration + full-pass verdicts

This plan is `autonomous: false` because the subagent can run unattended but the human (Donovan) must (a) confirm the calibration_results.agreement_count is 5 before proceeding to the full pass and (b) review every PRUNE and UNCERTAIN verdict in Plan 02 before the prune execution lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-skill-audit-prune-rc-gate/01-CONTEXT.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@packages/skills/python-patterns/SKILL.md
@packages/skills/golang-patterns/SKILL.md
@packages/skills/tdd-workflow/SKILL.md
@packages/skills/e2e-testing/SKILL.md
@packages/skills/eval-harness/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture frozen reference snapshot SHA + author audit subagent prompt</name>
  <files>
    .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md
  </files>
  <read_first>
    .planning/phases/01-skill-audit-prune-rc-gate/01-CONTEXT.md — read in full, especially §D-04 through §D-18 and §canonical_refs §"Reference graph for clause (c) evaluation"
    packages/skills/python-patterns/SKILL.md — calibration anchor for "obvious prune"
    packages/skills/golang-patterns/SKILL.md — calibration anchor for "obvious prune"
    packages/skills/tdd-workflow/SKILL.md — calibration anchor for clause-c keep (7 referrers expected)
    packages/skills/e2e-testing/SKILL.md — calibration anchor for clause-c keep (2 referrers expected)
    packages/skills/eval-harness/SKILL.md — calibration borderline anchor (expected PRUNE)
    packages/skills/configure-ecc/SKILL.md — verified cruft anchor (D-08)
  </read_first>
  <action>
    **Step 1 — Use the locked reference snapshot SHA.**

    The reference snapshot SHA is the pre-phase scoping-correction commit, locked to the literal value:

    ```
    REFERENCE_SHA="8d7ef909312fcb8544eebb469f515da965c9b1c3"
    ```

    This is the commit landed before Phase 1 execution began. Its subject is exactly `docs(planning): correct v1.2 prune target from ~60 to 75-85`. The 5 files D-02 covers (`.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/research/SUMMARY.md`, `tests/install.test.js`) are already in their post-correction state at this commit and at every commit descended from it.

    This is the `reference_snapshot_sha` value that goes at the top of PRUNE-VERDICT.json (Task 3) AND that the subagent prompt cites as the snapshot it must evaluate against. Per D-13: clause (c) MUST be evaluated against the original 107-skill repo state, not the in-progress pruned state. The SHA captured here is the immutable reference. If the audit subagent processes skills alphabetically and somehow mutates state mid-audit, the snapshot SHA proves the evaluation was done against the frozen state.

    **Pre-condition check (verify the SHA is real and the subject matches the expected string):**

    ```bash
    REFERENCE_SHA="8d7ef909312fcb8544eebb469f515da965c9b1c3"
    git show --format='%s' -s "$REFERENCE_SHA" | grep -q "^docs(planning): correct v1.2 prune target from ~60 to 75-85$"
    ```

    If this check fails, abort — the reference commit is missing from the local repo (rebase / branch issue) and the audit cannot proceed against a snapshot that is not present locally.

    **Step 2 — Author the audit subagent prompt.**

    Write `.planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md` with the EXACT structure below. Do NOT paraphrase any of the locked CONTEXT.md text; copy the rubric clauses, the protected list, the calibration set, and the JSON schema verbatim.

    File structure:

    ```markdown
    # Phase 1 Audit Subagent Prompt

    **Reference snapshot SHA:** 8d7ef909312fcb8544eebb469f515da965c9b1c3
    **Captured:** {ISO-8601 timestamp}
    **Rubric version:** v1.2-phase1

    ## Your Job

    You are auditing 41 candidate skills from `packages/skills/` against a two-pass rubric. Your output is a `PRUNE-VERDICT.json` file matching the locked schema in §D-18 of CONTEXT.md. Every verdict row MUST include per-clause `verdict` + `evidence` + `lines_cited`, and clause (c) MUST include `checked_paths` for provenance.

    ## CRITICAL: Frozen Reference Snapshot

    Clause (c) of the training-duplicate rubric is "no internal references to this skill exist in donnyclaude's distribution." This evaluation MUST be done against the repo state at SHA `8d7ef909312fcb8544eebb469f515da965c9b1c3`, NOT against the in-progress pruned state.

    Concretely: if you process skills alphabetically and flag `plankton-code-quality` for removal before reaching `verification-loop`, the reference from plankton → verification-loop disappears from the referrer set, and verification-loop's clause-(c) evidence changes mid-audit. Snapshot the reference graph BEFORE making any prune decisions, then evaluate all 41 candidates against the frozen snapshot.

    Mechanism: Before evaluating any skill, run a single grep pass and write the result to a scratch file. Read the scratch file (not live grep output) when evaluating clause (c) for each candidate.

    ```bash
    grep -rn -l --include='*.md' '\b\(skill-name-pattern\)\b' \
      packages/agents/ packages/hooks/ packages/commands/ \
      packages/skills/*/SKILL.md packages/rules/ \
      > .planning/phases/01-skill-audit-prune-rc-gate/.reference-graph-snapshot.txt
    ```

    Adjust the grep regex to match the skill names you need to evaluate.

    ## Protected list (64 skills — DO NOT EVALUATE)

    These 64 skills are protected and pass through the audit unchanged:

    **60 gsd-* skills (structurally coupled to GSD workflow engine, D-06):**
    All 60 directories matching `packages/skills/gsd-*`.

    **4 non-gsd protected skills (D-04, D-05):**
    - `skill-stocktake` — Test A (harness meta-tooling: distribution auditor with quick-diff.sh + results.json)
    - `humanizer` — Test B (substantive anti-AI-voice taste encoding, v2.3.0)
    - `strategic-compact` — Test B + executable (suggest-compact.js PreToolUse hook)
    - `frontend-slides` — Test B (opinionated aesthetic taste claim, @zarazhangrui credit)

    Emit these in PRUNE-VERDICT.json with `category: "protected"` and `verdict: "KEEP"` and `clauses: null` — they bypass both passes.

    ## Cruft determinations (D-08, D-09)

    Two skills are removed as cruft, NOT via the rubric. Emit them in PRUNE-VERDICT.json with `category: "cruft"` and the sub-category as listed:

    - `configure-ecc` — `cruft:unrelated-project-installer` — Step 0 of its SKILL.md `git clone`s `https://github.com/affaan-m/everything-claude-code.git` into `/tmp`. It is an installer for a different project (ECC), not donnyclaude infrastructure.
    - `continuous-learning` OR `continuous-learning-v2` (whichever is the loser) — `cruft:version-superseded` — Read both SKILL.md files and identify which is the successor. Usually v2 unless v1 retains features v2 dropped. Record the loser as cruft and the winner as `category: "candidate"` (it advances to the rubric like any other candidate).

    ## The 41 rubric candidates

    Every skill in `packages/skills/` that is NOT in the protected 64 AND NOT in the cruft determinations advances to the rubric. Build the candidate list from:

    ```bash
    ls packages/skills | grep -v '^gsd-' | grep -vE '^(skill-stocktake|humanizer|strategic-compact|frontend-slides|configure-ecc)$'
    ```

    Then resolve `continuous-learning` vs `continuous-learning-v2` per above (the loser drops out of the candidate list and into the cruft list; the winner stays). The grep above returns 42 entries (107 − 60 gsd-* − 4 non-gsd protected − 1 configure-ecc); after continuous-learning resolution drops the loser into cruft, **resulting count: 41**. The candidate list includes the four skills downgraded from the original protection candidate list per D-07: `eval-harness`, `plankton-code-quality`, `mcp-server-patterns`, and `iterative-retrieval` — they go through the rubric like any other candidate.

    ## Pass 1 — Cruft filter (positive test) — per D-11

    For each candidate, ask:

    > "Does this skill encode workflow, opinion, integration, or knowledge that is meaningfully tied to donnyclaude's supported tools (Claude Code, MCP servers, GSD engine, the rule files for the 13 supported languages, or the agents/hooks shipped in the distribution)?"

    YES on any clause = passes the cruft filter and advances to Pass 2.
    NO on all clauses = cruft removal, logged with category `cruft:origin-drift` (or another cruft sub-category if obvious).

    Pay extra attention to skills with `origin: ECC` in the YAML frontmatter — that tag has hidden cruft once (`configure-ecc`).

    ## Pass 2 — Training-duplicate rubric (all three must be TRUE to PRUNE) — per D-12

    **Clause (a) — Duplicates training-data knowledge**
    Does this skill duplicate knowledge for a popular language/framework the model has extensive training-data coverage of?
    - PASS (a fails) = unique/rare/specialized content NOT in training data
    - FAIL (a triggers) = standard idioms covered extensively in training (e.g., python-patterns, golang-patterns, rust-patterns, kotlin-patterns, laravel-patterns, django-patterns, springboot-patterns, java-coding-standards, cpp-coding-standards)

    **Clause (b) — No donnyclaude-specific glue**
    Does the SKILL.md body reference donnyclaude-native agents, hooks, commands, rule files, MCP server conventions, or the GSD engine in a way that would break if the skill were removed?
    - PASS (b fails) = has donnyclaude-specific glue
    - FAIL (b triggers) = no donnyclaude-native references

    **Clause (c) — No internal references**
    Does anything else in donnyclaude's distribution reference this skill by name? Grep the FROZEN SNAPSHOT (not live state) across:
    - `packages/agents/`
    - `packages/hooks/`
    - `packages/commands/`
    - `packages/skills/*/SKILL.md`
    - `packages/rules/`
    PASS (c fails) = at least one referrer found
    FAIL (c triggers) = zero referrers in the frozen snapshot

    **Verdict logic:** ALL THREE clauses must FAIL (a fails AND b fails AND c fails) → `verdict: "PRUNE"`. Any clause PASSES → `verdict: "KEEP"` with the passing clause as the rationale. If you are uncertain on any clause, use `verdict: "UNCERTAIN"` and explain in the rationale field — UNCERTAIN verdicts are reviewed by a human in Plan 02.

    ## Worked examples (already-verified clause-c keeps from D-14)

    These are NOT calibration anchors — they are reference examples showing how clause (c) is supposed to fire to protect glue-wired generic skills:

    - `tdd-workflow` — fails (a) (generic TDD methodology) and fails (b) (no donnyclaude-native references in body). BUT clause (c) PASSES with 7 referrers: tdd-guide agent, tdd/go-test/cpp-test/kotlin-test commands, php/testing rule, commands/tdd.md body. Verdict: KEEP. Rationale: clause-c protection.

    - `e2e-testing` — fails (a) (generic Playwright) and fails (b) (no donnyclaude-native references in body). BUT clause (c) PASSES with 2 referrers: e2e-runner agent, frontend-slides skill. Verdict: KEEP. Rationale: clause-c protection.

    These two demonstrate the rubric working as designed — generic-content skills that ARE wired into donnyclaude's internal structure stay protected.

    ## Calibration set (5 skills, mandatory pre-flight)

    Before processing the 41 candidates, evaluate these 5 pinned skills and emit a `calibration_results` block in PRUNE-VERDICT.json. Compare your verdicts to the expected verdicts.

    | Skill | Expected verdict | Why this calibration anchor |
    |-------|------------------|------------------------------|
    | `python-patterns` | PRUNE | Pure language idioms; fails a, b, c |
    | `golang-patterns` | PRUNE | Same pattern as python-patterns |
    | `tdd-workflow` | KEEP | Tests clause-c protection (7 referrers) |
    | `e2e-testing` | KEEP | Tests clause-c protection (2 referrers, harder than tdd-workflow) |
    | `eval-harness` | PRUNE | Borderline: origin:ECC, generic EDD methodology, no donnyclaude glue, no spot-checked referrers |

    **Calibration gate:** If your `agreement_count` is 5/5, set `proceed: true` in the calibration_results block and continue to the full pass. If agreement is 4/5 or worse, set `proceed: false` and STOP. Do NOT process the 41 candidates. The human reviewer in Plan 02 will tighten the rubric prompt and re-run calibration before the full pass is allowed.

    ## Output: PRUNE-VERDICT.json schema (locked, D-18)

    Write your output to `.planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json` with this EXACT shape:

    ```json
    {
      "version": "1.0",
      "rubric_version": "v1.2-phase1",
      "generated_at": "{ISO-8601 timestamp}",
      "reference_snapshot_sha": "8d7ef909312fcb8544eebb469f515da965c9b1c3",
      "calibration_results": {
        "pinned_skills": ["python-patterns", "golang-patterns", "tdd-workflow", "e2e-testing", "eval-harness"],
        "expected_verdicts": {
          "python-patterns": "PRUNE",
          "golang-patterns": "PRUNE",
          "tdd-workflow": "KEEP",
          "e2e-testing": "KEEP",
          "eval-harness": "PRUNE"
        },
        "actual_verdicts": { "python-patterns": "...", "golang-patterns": "...", "tdd-workflow": "...", "e2e-testing": "...", "eval-harness": "..." },
        "agreement_count": 0,
        "proceed": false
      },
      "verdicts": [
        {
          "skill": "python-patterns",
          "category": "candidate",
          "verdict": "PRUNE",
          "clauses": {
            "a": { "verdict": "FAIL", "evidence": "Lines X-Y enumerate standard Python idioms covered extensively in training data", "lines_cited": [X, Y] },
            "b": { "verdict": "FAIL", "evidence": "No references to donnyclaude conventions; pure language reference", "lines_cited": [] },
            "c": { "verdict": "FAIL", "evidence": "grep against frozen snapshot returned 0 matches in referrer set", "checked_paths": ["packages/agents/", "packages/hooks/", "packages/commands/", "packages/skills/*/SKILL.md", "packages/rules/"], "referrer_lines": [] }
          },
          "recommendation": "PRUNE",
          "rationale": "Pure language reference, no donnyclaude glue, no internal references. Training-duplicate."
        }
      ]
    }
    ```

    Notes:
    - Every candidate verdict MUST have all three clauses populated, even if the verdict is KEEP after one clause passes
    - Cruft entries use `category: "cruft"` and `clauses: null`; their rationale field carries the cruft sub-category
    - Protected entries use `category: "protected"`, `verdict: "KEEP"`, `clauses: null`, and a one-line rationale ("60 gsd-* protected per D-06" or the specific D-05 anchor)
    - The `rationale` field for prune verdicts is short (1-2 sentences); the human reviewer in Plan 02 may rewrite it before it lands in PRUNE-LOG.md

    ## Asymmetric risk reminder (D-15)

    False-positive prunes (skills that should have been kept) are user-facing and harder to reverse. False-negative keeps (skills that should have been pruned) are recoverable in v1.3. When uncertain, use UNCERTAIN, not PRUNE. The human reviewer will resolve UNCERTAIN cases in Plan 02.
    ```

    Save the file. Do not commit yet — Task 2 and Task 3 produce more outputs that land in a single Plan 01 commit at the end.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md && grep -c 'reference_snapshot_sha' .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md && grep -c 'frozen snapshot' .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md && grep -F 'agreement_count' .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md` exists
    - File contains the literal string `reference_snapshot_sha` (verified via grep, count >= 2: header + JSON schema)
    - File contains the literal string `frozen snapshot` (verified via grep)
    - File contains the literal string `agreement_count` (verified via grep)
    - File contains the names of all 4 non-gsd protected skills: `skill-stocktake`, `humanizer`, `strategic-compact`, `frontend-slides` (verified via grep, all 4 must appear)
    - File contains all 5 calibration skill names: `python-patterns`, `golang-patterns`, `tdd-workflow`, `e2e-testing`, `eval-harness` (verified via grep, all 5 must appear)
    - File contains the literal string `cruft:unrelated-project-installer` (verified via grep — proves D-08 cruft category is embedded)
    - File contains the literal string `Pass 1 — Cruft filter` and `Pass 2 — Training-duplicate rubric` (verified via grep, two-pass structure confirmed)
    - File contains the literal string `proceed: false` in the calibration gate description (verified via grep)
    - File header captures the locked reference SHA literally: `grep -F 'Reference snapshot SHA: 8d7ef909312fcb8544eebb469f515da965c9b1c3' .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md` returns a match
    - The captured SHA's `git log` includes the pre-phase commit `8d7ef909312fcb8544eebb469f515da965c9b1c3` (subject: `docs(planning): correct v1.2 prune target from ~60 to 75-85`)
  </acceptance_criteria>
  <done>Audit subagent prompt written with frozen-snapshot SHA captured, all locked rubric clauses embedded verbatim, protected list and calibration set complete, PRUNE-VERDICT.json schema embedded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run audit subagent calibration pass + human gate before full pass</name>
  <files>
    See <what-built> and <how-to-verify> below for the artifacts produced and verified by this checkpoint.
  </files>
  <action>
    This is a checkpoint:human-verify task — see <what-built> for context and <how-to-verify> for step-by-step execution instructions. The human (Donovan) executes the steps and types the resume-signal phrase to release the next task.
  </action>
  <verify>
    See <acceptance_criteria> for the grep-verifiable conditions that prove this checkpoint passed. The human types the resume-signal phrase only after every acceptance criterion is met.
  </verify>
  <done>
    Donovan types the resume-signal phrase from the <resume-signal> field. All <acceptance_criteria> are satisfied. Next task is unblocked.
  </done>
  <what-built>
    Audit subagent runs the 5-skill calibration pre-flight first. Subagent reads `audit-subagent-prompt.md`, evaluates the 5 pinned calibration skills (python-patterns, golang-patterns, tdd-workflow, e2e-testing, eval-harness) against the rubric, and writes a partial `PRUNE-VERDICT.json` containing ONLY the `calibration_results` block (no `verdicts[]` populated yet).
  </what-built>
  <how-to-verify>
    1. Spawn the audit subagent with the prompt at `.planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md` and instruction: "Run the calibration pre-flight ONLY. Do NOT proceed to the 41-candidate pass. Write the calibration_results block to PRUNE-VERDICT.json and stop."

    2. After subagent completes, read `.planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json` and check:
       - `calibration_results.actual_verdicts` populated for all 5 pinned skills
       - `calibration_results.agreement_count` is an integer 0-5
       - `calibration_results.proceed` is `true` if and only if agreement_count == 5

    3. **Human gate:** Donovan reviews the actual_verdicts vs expected_verdicts manually. Per D-16: 5/5 agreement = proceed. 4/5 or worse = STOP, tighten the rubric prompt in `audit-subagent-prompt.md`, and re-run calibration. Iterate until 5/5.

    4. Once 5/5 lands, type "approved" to release Task 3 (the full 41-candidate pass).

    Verification commands:
    - `jq '.calibration_results.actual_verdicts' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json` — should show all 5 skills with PRUNE/KEEP/UNCERTAIN values
    - `jq '.calibration_results.agreement_count' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json` — should equal 5 before approval
    - `jq '.calibration_results.proceed' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json` — should be `true` before approval
  </how-to-verify>
  <read_first>
    .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md
    .planning/phases/01-skill-audit-prune-rc-gate/01-CONTEXT.md §D-15, §D-16, §D-17
    packages/skills/python-patterns/SKILL.md
    packages/skills/golang-patterns/SKILL.md
    packages/skills/tdd-workflow/SKILL.md
    packages/skills/e2e-testing/SKILL.md
    packages/skills/eval-harness/SKILL.md
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json` exists with `calibration_results` block populated
    - `jq '.calibration_results.actual_verdicts | length' PRUNE-VERDICT.json` returns `5`
    - `jq '.calibration_results.agreement_count' PRUNE-VERDICT.json` returns `5`
    - `jq '.calibration_results.proceed' PRUNE-VERDICT.json` returns `true`
    - `jq '.reference_snapshot_sha' PRUNE-VERDICT.json` returns a non-empty string matching `8d7ef909312fcb8544eebb469f515da965c9b1c3`
    - Human (Donovan) explicitly types "approved" before Task 3 runs
  </acceptance_criteria>
  <resume-signal>Type "approved" once 5/5 calibration agreement is confirmed and proceed=true.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run audit subagent full 41-candidate pass + commit PRUNE-VERDICT.json</name>
  <files>
    .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json
  </files>
  <read_first>
    .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md (the locked prompt)
    .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json (calibration block from Task 2)
    .planning/phases/01-skill-audit-prune-rc-gate/01-CONTEXT.md §D-10 through §D-18 and §canonical_refs §"Reference graph for clause (c) evaluation"
  </read_first>
  <action>
    Pre-condition check: `jq '.calibration_results.proceed' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json` must return `true`. If not, this task aborts — the human gate in Task 2 was not passed and the full pass is not authorized.

    **Step 1 — Spawn the audit subagent for the full 41-candidate pass.**

    Prompt the subagent with `audit-subagent-prompt.md` (already authored in Task 1) and the instruction: "Calibration is approved (proceed=true). Run the full 41-candidate pass now. Append every candidate verdict to the existing PRUNE-VERDICT.json's `verdicts[]` array. Also emit cruft entries for `configure-ecc` and the `continuous-learning` loser. Also emit protected entries for the 64 protected skills (60 gsd-* + 4 non-gsd) so the file is a complete record of what happens to every skill."

    The subagent must:
    1. First snapshot the reference graph by running the locked grep command from the prompt against the 41 candidate names. Write the result to `.planning/phases/01-skill-audit-prune-rc-gate/.reference-graph-snapshot.txt` (a scratch file used by clause-c evaluation).
    2. For each of the 41 candidates, run Pass 1 (cruft filter) then Pass 2 (a/b/c rubric, evaluating clause c against the scratch file, NOT live state).
    3. For each protected skill, emit `{category: "protected", verdict: "KEEP", clauses: null, rationale: "60 gsd-* protected per D-06"}` for gsd-* entries and the specific D-05 anchor for the 4 non-gsd protected.
    4. For configure-ecc: `{category: "cruft", verdict: "PRUNE", clauses: null, rationale: "Step 0 git clones https://github.com/affaan-m/everything-claude-code.git into /tmp — installer for unrelated ECC project, not donnyclaude infrastructure", sub_category: "cruft:unrelated-project-installer"}`.
    5. For the continuous-learning loser: `{category: "cruft", verdict: "PRUNE", clauses: null, rationale: "Superseded by continuous-learning-{v1|v2}", sub_category: "cruft:version-superseded"}` — subagent reads both files and identifies the loser.
    6. Write the complete file in one atomic write operation (do not append iteratively) so the final JSON is well-formed.

    **Step 2 — Validate the JSON output.**

    Run these checks. If any fails, the subagent's output is broken and must be regenerated:

    ```bash
    # JSON validity
    jq '.' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json > /dev/null

    # Calibration block intact
    test "$(jq '.calibration_results.agreement_count' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json)" = "5"

    # All 107 skills accounted for (60 gsd protected + 4 non-gsd protected + 2 cruft + 41 candidates = 107 entries). Both continuous-learning v1 and v2 ARE recorded: winner as category=candidate, loser as category=cruft — still totals 107.
    test "$(jq '.verdicts | length' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json)" -ge 107

    # Reference snapshot SHA matches what's on disk
    SNAPSHOT_SHA=$(jq -r '.reference_snapshot_sha' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json)
    git log --oneline "$SNAPSHOT_SHA" -1 | grep -F 'docs(planning): correct v1.2 prune target from ~60 to 75-85'

    # Every candidate has all three clauses populated
    test "$(jq '[.verdicts[] | select(.category == "candidate") | select(.clauses == null)] | length' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json)" = "0"
    ```

    **Step 3 — Commit the audit artifacts.**

    Atomic commit via gsd-tools:
    ```
    node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(phase-01): land Phase 1 audit artifacts (subagent prompt + PRUNE-VERDICT.json)

    Audit subagent prompt with frozen-snapshot SHA + locked rubric.
    PRUNE-VERDICT.json with 5/5 calibration agreement and full pass over
    41 candidates + 64 protected + cruft entries.

    Calibration: {agreement_count}/5 against the pinned 5-skill set.
    Reference snapshot: {short SHA}.

    Next: human review of PRUNE/UNCERTAIN verdicts in Plan 02." \\
      --files .planning/phases/01-skill-audit-prune-rc-gate/audit-subagent-prompt.md .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json
    ```

    Note: do NOT commit `.reference-graph-snapshot.txt` — it is a scratch file used during the audit and is not part of the audit trail. Either delete it or add to `.gitignore` if it might recur.
  </action>
  <verify>
    <automated>jq '.' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json > /dev/null && test "$(jq '.calibration_results.agreement_count' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json)" = "5" && test "$(jq '.verdicts | length' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json)" -ge 107 && test "$(jq '[.verdicts[] | select(.category == "candidate") | select(.clauses == null)] | length' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.' .planning/phases/01-skill-audit-prune-rc-gate/PRUNE-VERDICT.json > /dev/null` exits 0 (well-formed JSON)
    - `jq '.calibration_results.agreement_count' PRUNE-VERDICT.json` returns `5`
    - `jq '.calibration_results.proceed' PRUNE-VERDICT.json` returns `true`
    - `jq '.verdicts | length' PRUNE-VERDICT.json` returns a number `>= 107` (every skill in `packages/skills/` is accounted for)
    - `jq '[.verdicts[] | select(.category == "candidate")] | length' PRUNE-VERDICT.json` returns `41` (exactly the rubric candidate count)
    - `jq '[.verdicts[] | select(.category == "protected")] | length' PRUNE-VERDICT.json` returns `64` (60 gsd-* + 4 non-gsd)
    - `jq '[.verdicts[] | select(.category == "cruft")] | length' PRUNE-VERDICT.json` returns `>= 2` (configure-ecc + continuous-learning loser)
    - `jq '[.verdicts[] | select(.category == "candidate") | select(.clauses == null)] | length' PRUNE-VERDICT.json` returns `0` (every candidate has clauses populated)
    - `jq -r '.reference_snapshot_sha' PRUNE-VERDICT.json` returns a SHA whose ancestry includes commit `8d7ef909312fcb8544eebb469f515da965c9b1c3`
    - `git log -1 --format='%s'` returns `feat(phase-01): land Phase 1 audit artifacts (subagent prompt + PRUNE-VERDICT.json)` (or matches that prefix)
    - `git show --stat HEAD` lists exactly `audit-subagent-prompt.md` and `PRUNE-VERDICT.json`
  </acceptance_criteria>
  <done>Full audit pass complete; PRUNE-VERDICT.json committed with calibration + 41 candidate verdicts + 64 protected entries + cruft entries; reference snapshot SHA embedded; ready for human review pass in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit subagent → PRUNE-VERDICT.json | Subagent emits structured JSON that becomes input to human review and (after review) prune execution |
| in-progress audit → reference graph | Order-dependence gotcha (D-13): subagent must evaluate clause c against frozen snapshot, not live state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Reference graph mid-audit drift | mitigate | D-13 frozen-snapshot mechanism: subagent writes scratch file BEFORE evaluating clause c, then reads scratch file (not live grep). Acceptance criterion verifies `reference_snapshot_sha` matches a real commit |
| T-01-02 | Information Disclosure | Audit prompt or PRUNE-VERDICT.json containing secrets | accept | Both files are pure metadata about packages/skills/ directory contents — no PII, no credentials, no API keys touched |
| T-01-03 | Tampering | Calibration gate bypass | mitigate | Task 3 pre-condition check requires `proceed: true`; gsd-tools commit will not run if calibration is incomplete; Task 2 is a checkpoint:human-verify gate (Donovan must explicitly approve) |
| T-01-04 | Repudiation | False-positive prunes (skills wrongly cut) | mitigate | Asymmetric-risk policy (D-15): subagent MUST use UNCERTAIN when in doubt; human reviews every PRUNE and UNCERTAIN in Plan 02 before any git mv operations |
</threat_model>

<verification>
After Plan 01 commit lands:
1. `git log -1 --stat` shows exactly 2 files in the latest commit: audit-subagent-prompt.md, PRUNE-VERDICT.json
2. `jq '.calibration_results.proceed' PRUNE-VERDICT.json` returns `true`
3. `jq '[.verdicts[] | select(.category == "candidate")] | length' PRUNE-VERDICT.json` returns `41`
4. `jq -r '.reference_snapshot_sha' PRUNE-VERDICT.json` returns the SHA captured at the start of Task 1
5. The reference SHA's `git log` includes the pre-phase commit `8d7ef909312fcb8544eebb469f515da965c9b1c3` (proves the snapshot is post-correction)
</verification>

<success_criteria>
Plan 01 complete when:
- `audit-subagent-prompt.md` is committed with frozen-snapshot SHA + locked rubric verbatim
- `PRUNE-VERDICT.json` is committed with calibration agreement_count=5, proceed=true, and verdicts[] populated for all 107 skills (41 candidates + 64 protected + cruft entries)
- Every candidate verdict has all three clauses (a/b/c) populated with verdict + evidence + lines_cited
- Clause (c) was evaluated against the frozen snapshot, not live state
- Plan 02 (human review) has the input it needs to begin
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-audit-prune-rc-gate/01-01-SUMMARY.md` recording: reference snapshot SHA, calibration agreement count (must be 5), candidate count (must be 41), protected count (must be 64), cruft count (must be 2), total PRUNE verdicts, total KEEP verdicts, total UNCERTAIN verdicts, the commit SHA of the Plan 01 commit.
</output>
