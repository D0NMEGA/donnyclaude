# DonnyClaude v1.3 — Backlog (paused)

**Status:** Backlog. Renumbered from v1.2 on 2026-05-08 when v1.2 closed without shipping its full scope. donnyclaude itself is in **maintenance mode** while the active substrate-research work moves to the separate `harness-lab` repo per `harness-pivot-v1` (see `.planning/research/ahol/MILESTONE-PLAN-harness-pivot-v1.md`).

**Reason v1.2 closed:** The work originally scoped as "v1.2 Harness Optimization" was, on inspection during the AHOL post-mortem (`.planning/research/ahol/AHOL-POSTMORTEM.md`), revealed to be donnyclaude-package maintenance — not harness optimization in the rigorous methodology sense. The label was wrong; the work itself is still useful but is appropriately renumbered as donnyclaude maintenance backlog rather than as a research milestone.

**When this resumes:** When `harness-pivot-v1` reaches Phase 27 (milestone close-out) or earlier if a donnyclaude maintenance window opens. v1.3 work is not gated on `harness-pivot-v1`'s success or failure.

---

## Carried-forward scope (from former v1.2)

The following items were the v1.2 active scope at HEAD `bf6dd7a` (when v1.2 closed). They remain valuable as donnyclaude maintenance and are preserved verbatim here.

### Phase 1: Skill Audit + Prune (RC GATE)

- [ ] **SKILLS-01**: ship v1.2.0-rc1 as a cruft-only prune (107→105, removing configure-ecc and the continuous-learning loser); the broader training-duplicate prune scoped to 75-85 skills was deferred to a future cycle after the calibration pre-flight surfaced that the rubric's clause (c) cannot distinguish training-duplicate skills from catalog cross-links in the current codebase. See `.planning/phases/01-skill-audit-prune-rc-gate/01-CONTEXT.md#Corrections` (2026-04-13) and `.planning/research/v1.3-seeds/` for preserved audit artifacts.
- [ ] **Skill prune rubric redesign + aggressive prune** — needs research pass on "what does 'semantic dependency' mean in a catalog-linked distribution?" Possible angles: runtime load signals (after progressive disclosure ships), agent system-prompt content embedding vs cross-reference pointers, direct training-data overlap measurement rather than referrer counting, category-level decisions rather than per-skill rubric. Seeds: `.planning/research/v1.3-seeds/`.

### Phase 2: Install Manifest + Progressive Disclosure

- [ ] **SKILLS-02**: `npx donnyclaude` install writes `~/.claude/.donnyclaude-manifest.json` with file list, checksums, and version
- [ ] **SKILLS-03**: install builds a description-indexed skill registry enabling progressive disclosure (avoid the ~100-tool degradation cliff)
- [ ] **SKILLS-04**: user can enable/disable individual skills via `settings.json` (`skills.enabled[]` / `skills.disabled[]`)

### Phase 3: Subagent Return Contracts

- [ ] **AGENTS-01**: ~29 open-ended domain subagents (architect, planner, code-reviewer, tdd-guide, refactor-cleaner, etc.) receive explicit "return only X" contracts matching the established GSD pattern.

### Phase 4: Hook Backup/Restore Subsystem

- [ ] **HOOKS-01**: SessionStart hook is a real testable script (not the inline 300-char shell one-liner currently in `hooks.json:132-143`) that injects `git branch`, `git diff --stat`, env discovery, and most-recent backup path
- [ ] **HOOKS-02**: PreCompact hook actively backs up state, file paths, and test status to `.claude/backups/{timestamp}/` before context summarization
- [ ] **HOOKS-03**: SessionStart hook restores the most-recent backup when one exists

### Phase 5: Stop Verification Subagent

- [ ] **HOOKS-04**: Stop hook spawns a verification subagent that gates session close, blocking premature task-complete declarations.

### Stretch (would have been deferred from v1.2 to v1.3 anyway)

- [ ] **PostToolUse verification hook on Write/Edit** — needs project-aware linter/test detection (Python vs Node vs Rust vs mixed), strategy for handling 2-30s test runs without injecting them into every edit, and a kill-switch for known-broken states.
- [ ] **Reasoning sandwich for long-running subagents** — depends on AGENTS-01 (return-contract enforcement) being solid first.
- [ ] **Proactive 60% compaction override** — contingent on HOOKS-02 (active PreCompact backup) actually working end-to-end first.
- [ ] **Migrate top 10-15 highest-frequency rules from rule files to hooks** — requires telemetry that doesn't exist yet.

---

## Phase artifacts preserved

The following files from v1.2's in-flight work are retained, NOT moved or deleted:

- `.planning/phases/01-skill-audit-prune-rc-gate/` — Phase 1 working directory with the rubric-deferral CONTEXT.md and preserved partial audit artifacts.
- `.planning/research/v1.3-seeds/` — partial audit artifacts and reference graph snapshot from the calibration pre-flight that fired and deferred the rubric.
- `.planning/research/DEEP-RESEARCH.md`, `INVENTORY.md`, `SUMMARY.md` — the v1.2 scoping research bundle. Still valid as donnyclaude maintenance reference; not reused for `harness-pivot-v1`.

When v1.3 resumes, start by reviewing the rubric-redesign seeds and the donnyclaude maintenance items above.

---

*Renumbered from v1.2 on 2026-05-08 at HEAD `bf6dd7a` as part of the AHOL milestone close. donnyclaude is paused; substrate research moves to harness-lab.*
